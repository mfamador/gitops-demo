apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hello
spec:
  values:
    image:
      tag: 1.0.4 # {"$imagepolicy": "flux-system:hello:tag"}
      automation:
        semver:
          expr: 1.0.x
