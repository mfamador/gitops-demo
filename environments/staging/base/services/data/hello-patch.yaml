apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: hello
spec:
  values:
    image:
      tag: 1.0.1 # {"$imagepolicy": "{{ .Values.image.automation.namespace }}:{{ include "api.name" . }}"}
      automation:
        semver:
          expr: 1.0.x
